# Astro pins sharp <0.35, which carries the libvips CVEs (GHSA / CVE-2026-33327,
# -33328, -35590, -35591). Astro's image optimization is unused here, so every
# copy is aligned to the patched line.
overrides:
  sharp: ^0.35.0
